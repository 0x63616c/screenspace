# Go Foundation Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Replace the flat `server/` package layout with a proper `internal/` structure, swap `lib/pq`+`database/sql` for `pgx/v5`+pgxpool, generate the repository layer with sqlc, define the API contract in OpenAPI, centralize config, and wire everything with a Makefile.

**Architecture:** All business logic moves into `internal/` with one package per concern (`config`, `handler`, `service`, `respond`, `middleware`, `storage`, `types`). The repository layer is fully generated by sqlc from SQL query files in `db/queries/`; no hand-written SQL survives. The OpenAPI spec in `api/openapi.yaml` is the single source of truth for all HTTP types and generates the chi server interface via oapi-codegen.

**Tech Stack:** Go 1.26, pgx/v5+pgxpool, sqlc (pgx/v5 driver), oapi-codegen (chi-server mode), chi v5, golang-migrate (pgx driver), slog, golangci-lint.

---

## Current State

The server lives at `server/` as a flat `main` package with:
- `main.go`, `config.go`, `migrate.go` — all in `package main`
- `handler/`, `repository/`, `service/`, `middleware/`, `storage/` — separate packages but no `internal/` boundary
- `lib/pq` + `database/sql` throughout (`repository/user.go`, `repository/wallpaper.go`, `repository/favorite.go`, `repository/report.go`, `migrate.go`)
- `pq.Array()` used in `wallpaper.go` and `favorite.go` for `TEXT[]` scanning
- No Makefile, no sqlc, no OpenAPI spec, no `internal/` directory
- Migrations live at `server/migrations/` (embedded via `//go:embed`)
- `go.mod` module: `github.com/0x63616c/screenspace/server`

**Target layout after this plan:**

```
server/
  api/
    openapi.yaml
    cfg.yaml                     # oapi-codegen config
  cmd/
    server/
      main.go                    # wiring only
  db/
    sqlc.yaml
    queries/
      user.sql
      wallpaper.sql
      favorite.sql
      report.sql
    generated/                   # sqlc output (committed)
      db.go
      models.go
      querier.go
      user.sql.go
      wallpaper.sql.go
      favorite.sql.go
      report.sql.go
    migrations/                  # moved from server/migrations/
      001_initial.up.sql
      001_initial.down.sql
      002_add_rejection_reason.up.sql
      002_add_rejection_reason.down.sql
  generated/                     # oapi-codegen output (committed)
    server.gen.go
    types.gen.go
  internal/
    config/
      config.go
    handler/
      (unchanged for now — handlers refactored in later plans)
    middleware/
      (unchanged for now)
    respond/
      respond.go
    service/
      (unchanged for now)
    storage/
      (unchanged for now)
    types/
      types.go
  Makefile
  go.mod                         # updated: pgx/v5, chi, oapi-codegen, sqlc tools
  go.sum
```

> **Note:** The `repository/` package is deleted as part of the atomic pgx swap. Handlers that reference `repository.*` types are updated to reference `db.*` (sqlc-generated) types. This is all one commit (Task 4).

---

## Task 1 — Create directory scaffold and move migrations

**Files:**
- Create: `server/api/` (directory)
- Create: `server/cmd/server/` (directory)
- Create: `server/db/queries/` (directory)
- Create: `server/db/generated/` (directory, gitkeep)
- Create: `server/generated/` (directory, gitkeep)
- Create: `server/internal/config/` (directory)
- Create: `server/internal/respond/` (directory)
- Create: `server/internal/types/` (directory)
- Move: `server/migrations/` -> `server/db/migrations/`

### Steps

- [ ] Create the directory structure:

```bash
cd /path/to/screenspace/server
mkdir -p api
mkdir -p cmd/server
mkdir -p db/queries
mkdir -p db/generated
mkdir -p generated
mkdir -p internal/config
mkdir -p internal/respond
mkdir -p internal/types
```

- [ ] Move migrations:

```bash
mv migrations db/migrations
```

- [ ] Update the embed path in `migrate.go` (line 16) from `migrations/*.sql` to `db/migrations/*.sql`. Also update the `iofs.New` call (line 20) from `"migrations"` to `"db/migrations"`:

Current `server/migrate.go` lines 15-21:
```go
//go:embed migrations/*.sql
var migrationsFS embed.FS

func RunMigrations(db *sql.DB) error {
	source, err := iofs.New(migrationsFS, "migrations")
```

Replace with:
```go
//go:embed db/migrations/*.sql
var migrationsFS embed.FS

func RunMigrations(db *sql.DB) error {
	source, err := iofs.New(migrationsFS, "db/migrations")
```

- [ ] Verify the build still compiles (migrations path is the only change):

```bash
cd server && go build ./...
# Expected: no output (success)
```

- [ ] Commit:

```bash
git add server/db/migrations server/migrate.go
git rm -r --cached server/migrations
git commit -m "chore(server): move migrations to db/migrations and update embed path"
git push
```

---

## Task 2 — Write `api/openapi.yaml`

**Files:**
- Create: `server/api/openapi.yaml`
- Create: `server/api/cfg.yaml`

The spec covers all routes currently in `main.go` lines 69-102. Request/response shapes are derived from existing handler structs.

### Steps

- [ ] Create `server/api/openapi.yaml`:

```yaml
openapi: "3.0.3"
info:
  title: ScreenSpace API
  version: "1.0.0"

servers:
  - url: /api/v1

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT

  schemas:
    Error:
      type: object
      required: [error]
      properties:
        error:
          type: object
          required: [code, message]
          properties:
            code:
              type: string
            message:
              type: string

    PaginatedMeta:
      type: object
      required: [total, limit, offset]
      properties:
        total:
          type: integer
        limit:
          type: integer
        offset:
          type: integer

    User:
      type: object
      required: [id, email, role, banned, created_at]
      properties:
        id:
          type: string
          format: uuid
        email:
          type: string
          format: email
        role:
          $ref: "#/components/schemas/UserRole"
        banned:
          type: boolean
        created_at:
          type: string
          format: date-time

    UserRole:
      type: string
      enum: [user, admin]

    Wallpaper:
      type: object
      required: [id, title, uploader_id, status, tags, resolution, width, height, duration, file_size, format, download_count, storage_key, thumbnail_url, preview_url, created_at, updated_at]
      properties:
        id:
          type: string
          format: uuid
        title:
          type: string
        uploader_id:
          type: string
          format: uuid
        status:
          $ref: "#/components/schemas/WallpaperStatus"
        category:
          type: string
        tags:
          type: array
          items:
            type: string
        resolution:
          type: string
        width:
          type: integer
        height:
          type: integer
        duration:
          type: number
          format: double
        file_size:
          type: integer
          format: int64
        format:
          type: string
        download_count:
          type: integer
          format: int64
        storage_key:
          type: string
        thumbnail_url:
          type: string
        preview_url:
          type: string
        rejection_reason:
          type: string
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time

    WallpaperStatus:
      type: string
      enum: [pending, pending_review, approved, rejected]

    WallpaperCategory:
      type: string
      enum: [nature, abstract, urban, cinematic, space, underwater, minimal, other]

    Report:
      type: object
      required: [id, wallpaper_id, reporter_id, reason, status, created_at]
      properties:
        id:
          type: string
          format: uuid
        wallpaper_id:
          type: string
          format: uuid
        reporter_id:
          type: string
          format: uuid
        reason:
          type: string
        status:
          type: string
        created_at:
          type: string
          format: date-time

    RegisterRequest:
      type: object
      required: [email, password]
      properties:
        email:
          type: string
          format: email
        password:
          type: string
          minLength: 8

    LoginRequest:
      type: object
      required: [email, password]
      properties:
        email:
          type: string
          format: email
        password:
          type: string

    AuthResponse:
      type: object
      required: [token, role]
      properties:
        token:
          type: string
        role:
          $ref: "#/components/schemas/UserRole"

    MeResponse:
      type: object
      required: [id, email, role]
      properties:
        id:
          type: string
          format: uuid
        email:
          type: string
          format: email
        role:
          $ref: "#/components/schemas/UserRole"

    CreateWallpaperRequest:
      type: object
      required: [title]
      properties:
        title:
          type: string
          maxLength: 255
        category:
          $ref: "#/components/schemas/WallpaperCategory"
        tags:
          type: array
          items:
            type: string
            maxLength: 50
          maxItems: 10

    CreateWallpaperResponse:
      type: object
      required: [id, upload_url]
      properties:
        id:
          type: string
          format: uuid
        upload_url:
          type: string

    ReportRequest:
      type: object
      required: [reason]
      properties:
        reason:
          type: string
          maxLength: 500

    RejectRequest:
      type: object
      required: [reason]
      properties:
        reason:
          type: string

    EditWallpaperRequest:
      type: object
      properties:
        title:
          type: string
          maxLength: 255
        category:
          $ref: "#/components/schemas/WallpaperCategory"
        tags:
          type: array
          items:
            type: string
            maxLength: 50
          maxItems: 10

    WallpaperListResponse:
      allOf:
        - $ref: "#/components/schemas/PaginatedMeta"
        - type: object
          required: [items]
          properties:
            items:
              type: array
              items:
                $ref: "#/components/schemas/Wallpaper"

    UserListResponse:
      allOf:
        - $ref: "#/components/schemas/PaginatedMeta"
        - type: object
          required: [items]
          properties:
            items:
              type: array
              items:
                $ref: "#/components/schemas/User"

    ReportListResponse:
      allOf:
        - $ref: "#/components/schemas/PaginatedMeta"
        - type: object
          required: [items]
          properties:
            items:
              type: array
              items:
                $ref: "#/components/schemas/Report"

    CategoryListResponse:
      type: object
      required: [categories]
      properties:
        categories:
          type: array
          items:
            $ref: "#/components/schemas/WallpaperCategory"

    HealthResponse:
      type: object
      required: [status]
      properties:
        status:
          type: string
        db:
          type: string
        uptime:
          type: string

paths:
  /health:
    get:
      operationId: GetHealth
      summary: Health check
      tags: [system]
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthResponse"

  /categories:
    get:
      operationId: ListCategories
      summary: List all categories
      tags: [wallpapers]
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CategoryListResponse"

  /wallpapers:
    get:
      operationId: ListWallpapers
      summary: List approved wallpapers
      tags: [wallpapers]
      parameters:
        - name: sort
          in: query
          schema:
            type: string
            enum: [recent, popular]
        - name: category
          in: query
          schema:
            $ref: "#/components/schemas/WallpaperCategory"
        - name: q
          in: query
          schema:
            type: string
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
            maximum: 100
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WallpaperListResponse"
    post:
      operationId: CreateWallpaper
      summary: Initiate wallpaper upload
      tags: [wallpapers]
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateWallpaperRequest"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateWallpaperResponse"
        "400":
          description: Bad Request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"

  /wallpapers/popular:
    get:
      operationId: ListWallpapersPopular
      summary: List wallpapers by popularity
      tags: [wallpapers]
      parameters:
        - name: category
          in: query
          schema:
            $ref: "#/components/schemas/WallpaperCategory"
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
            maximum: 100
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WallpaperListResponse"

  /wallpapers/recent:
    get:
      operationId: ListWallpapersRecent
      summary: List wallpapers by recency
      tags: [wallpapers]
      parameters:
        - name: category
          in: query
          schema:
            $ref: "#/components/schemas/WallpaperCategory"
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
            maximum: 100
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WallpaperListResponse"

  /wallpapers/{id}:
    get:
      operationId: GetWallpaper
      summary: Get a single wallpaper
      tags: [wallpapers]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Wallpaper"
        "404":
          description: Not Found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"

  /wallpapers/{id}/download:
    post:
      operationId: DownloadWallpaper
      summary: Increment download count and get download URL
      tags: [wallpapers]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: object
                required: [url]
                properties:
                  url:
                    type: string
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not Found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"

  /wallpapers/{id}/favorite:
    post:
      operationId: ToggleFavorite
      summary: Toggle wallpaper favorite
      tags: [favorites]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: object
                required: [favorited]
                properties:
                  favorited:
                    type: boolean
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"

  /wallpapers/{id}/report:
    post:
      operationId: ReportWallpaper
      summary: Report a wallpaper
      tags: [reports]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReportRequest"
      responses:
        "201":
          description: Created
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"

  /wallpapers/{id}/finalize:
    post:
      operationId: FinalizeWallpaper
      summary: Finalize wallpaper upload and trigger processing
      tags: [wallpapers]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Wallpaper"
        "400":
          description: Bad Request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not Found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"

  /auth/register:
    post:
      operationId: Register
      summary: Register a new user
      tags: [auth]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RegisterRequest"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthResponse"
        "400":
          description: Bad Request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Conflict
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"

  /auth/login:
    post:
      operationId: Login
      summary: Login
      tags: [auth]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LoginRequest"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"

  /auth/me:
    get:
      operationId: GetMe
      summary: Get current user
      tags: [auth]
      security:
        - bearerAuth: []
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MeResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"

  /me/favorites:
    get:
      operationId: ListFavorites
      summary: List current user's favorited wallpapers
      tags: [favorites]
      security:
        - bearerAuth: []
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
            maximum: 100
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WallpaperListResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"

  /admin/queue:
    get:
      operationId: GetAdminQueue
      summary: List wallpapers pending review
      tags: [admin]
      security:
        - bearerAuth: []
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
            maximum: 100
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WallpaperListResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"

  /admin/queue/{id}/approve:
    post:
      operationId: ApproveWallpaper
      summary: Approve a wallpaper
      tags: [admin]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Wallpaper"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not Found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"

  /admin/queue/{id}/reject:
    post:
      operationId: RejectWallpaper
      summary: Reject a wallpaper
      tags: [admin]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RejectRequest"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Wallpaper"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not Found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"

  /admin/wallpapers:
    get:
      operationId: AdminListWallpapers
      summary: Admin list all wallpapers
      tags: [admin]
      security:
        - bearerAuth: []
      parameters:
        - name: status
          in: query
          schema:
            $ref: "#/components/schemas/WallpaperStatus"
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
            maximum: 100
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WallpaperListResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"

  /admin/wallpapers/{id}:
    patch:
      operationId: AdminEditWallpaper
      summary: Edit wallpaper metadata
      tags: [admin]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EditWallpaperRequest"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Wallpaper"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not Found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"

  /admin/users:
    get:
      operationId: AdminListUsers
      summary: Admin list all users
      tags: [admin]
      security:
        - bearerAuth: []
      parameters:
        - name: q
          in: query
          schema:
            type: string
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
            maximum: 100
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserListResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"

  /admin/users/{id}/ban:
    post:
      operationId: BanUser
      summary: Ban a user
      tags: [admin]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: OK
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not Found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"

  /admin/users/{id}/unban:
    post:
      operationId: UnbanUser
      summary: Unban a user
      tags: [admin]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: OK
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not Found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"

  /admin/users/{id}/promote:
    post:
      operationId: PromoteUser
      summary: Promote a user to admin
      tags: [admin]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: OK
        "400":
          description: Bad Request (e.g. user is banned)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not Found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"

  /admin/reports:
    get:
      operationId: AdminListReports
      summary: Admin list pending reports
      tags: [admin]
      security:
        - bearerAuth: []
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
            maximum: 100
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReportListResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"

  /admin/reports/{id}/dismiss:
    post:
      operationId: DismissReport
      summary: Dismiss a report
      tags: [admin]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: OK
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not Found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
```

- [ ] Create `server/api/cfg.yaml` (oapi-codegen config):

```yaml
package: generated
output: ../generated/server.gen.go
generate:
  chi-server: true
  models: true
  strict-server: false
```

- [ ] Commit:

```bash
git add server/api/
git commit -m "feat(server): add OpenAPI 3.0 spec and oapi-codegen config"
git push
```

---

## Task 3 — Write `db/sqlc.yaml` and all SQL query files

**Files:**
- Create: `server/db/sqlc.yaml`
- Create: `server/db/queries/user.sql`
- Create: `server/db/queries/wallpaper.sql`
- Create: `server/db/queries/favorite.sql`
- Create: `server/db/queries/report.sql`

### Steps

- [ ] Create `server/db/sqlc.yaml`:

```yaml
version: "2"
sql:
  - engine: "postgresql"
    queries: "queries/"
    schema: "migrations/"
    gen:
      go:
        package: "db"
        out: "generated"
        sql_package: "pgx/v5"
        emit_interface: true
        emit_json_tags: true
        emit_pointers_for_null_types: true
        overrides:
          - db_type: "uuid"
            go_type: "github.com/google/uuid.UUID"
          - db_type: "pg_catalog.text[]"
            go_type: "[]string"
```

- [ ] Create `server/db/queries/user.sql`:

```sql
-- name: CreateUser :one
INSERT INTO users (email, password_hash, role)
VALUES ($1, $2, $3)
RETURNING id, email, password_hash, role, banned, created_at;

-- name: GetUserByID :one
SELECT id, email, password_hash, role, banned, created_at
FROM users
WHERE id = $1;

-- name: GetUserByEmail :one
SELECT id, email, password_hash, role, banned, created_at
FROM users
WHERE email = $1;

-- name: ListUsers :many
SELECT id, email, password_hash, role, banned, created_at
FROM users
ORDER BY created_at DESC
LIMIT sqlc.arg('lim') OFFSET sqlc.arg('off');

-- name: CountUsers :one
SELECT COUNT(*) FROM users;

-- name: ListUsersWithSearch :many
SELECT id, email, password_hash, role, banned, created_at
FROM users
WHERE email ILIKE sqlc.arg('query')
ORDER BY created_at DESC
LIMIT sqlc.arg('lim') OFFSET sqlc.arg('off');

-- name: CountUsersWithSearch :one
SELECT COUNT(*) FROM users
WHERE email ILIKE sqlc.arg('query');

-- name: SetBanned :exec
UPDATE users SET banned = $1 WHERE id = $2;

-- name: SetRole :exec
UPDATE users SET role = $1 WHERE id = $2;
```

- [ ] Create `server/db/queries/wallpaper.sql`:

```sql
-- name: CreateWallpaper :one
INSERT INTO wallpapers (title, uploader_id, storage_key, resolution, width, height, duration, file_size, format, thumbnail_key, preview_key)
VALUES ($1, $2, $3, '', 0, 0, 0, 0, '', '', '')
RETURNING id, title, uploader_id, status, COALESCE(category, '') AS category, tags,
          resolution, width, height, duration, file_size, format, download_count,
          storage_key, thumbnail_key, preview_key, rejection_reason, created_at, updated_at;

-- name: GetWallpaperByID :one
SELECT id, title, uploader_id, status, COALESCE(category, '') AS category, tags,
       resolution, width, height, duration, file_size, format, download_count,
       storage_key, thumbnail_key, preview_key, rejection_reason, created_at, updated_at
FROM wallpapers
WHERE id = $1;

-- name: ListWallpapersRecent :many
SELECT id, title, uploader_id, status, COALESCE(category, '') AS category, tags,
       resolution, width, height, duration, file_size, format, download_count,
       storage_key, thumbnail_key, preview_key, rejection_reason, created_at, updated_at
FROM wallpapers
WHERE status = sqlc.arg('status')
  AND (sqlc.narg('category')::text IS NULL OR category ILIKE sqlc.narg('category'))
  AND (sqlc.narg('query')::text IS NULL OR title ILIKE sqlc.narg('query'))
ORDER BY created_at DESC
LIMIT sqlc.arg('lim') OFFSET sqlc.arg('off');

-- name: ListWallpapersPopular :many
SELECT id, title, uploader_id, status, COALESCE(category, '') AS category, tags,
       resolution, width, height, duration, file_size, format, download_count,
       storage_key, thumbnail_key, preview_key, rejection_reason, created_at, updated_at
FROM wallpapers
WHERE status = sqlc.arg('status')
  AND (sqlc.narg('category')::text IS NULL OR category ILIKE sqlc.narg('category'))
  AND (sqlc.narg('query')::text IS NULL OR title ILIKE sqlc.narg('query'))
ORDER BY download_count DESC, created_at DESC
LIMIT sqlc.arg('lim') OFFSET sqlc.arg('off');

-- name: CountWallpapers :one
SELECT COUNT(*)
FROM wallpapers
WHERE status = sqlc.arg('status')
  AND (sqlc.narg('category')::text IS NULL OR category ILIKE sqlc.narg('category'))
  AND (sqlc.narg('query')::text IS NULL OR title ILIKE sqlc.narg('query'));

-- name: UpdateWallpaperStatus :exec
UPDATE wallpapers
SET status = $1, updated_at = now()
WHERE id = $2;

-- name: UpdateWallpaperStatusWithReason :exec
UPDATE wallpapers
SET status = $1, rejection_reason = $2, updated_at = now()
WHERE id = $3;

-- name: UpdateWallpaperMetadata :exec
UPDATE wallpapers
SET title = $1, category = $2, tags = $3, updated_at = now()
WHERE id = $4;

-- name: UpdateWallpaperAfterFinalize :one
UPDATE wallpapers
SET width = $1,
    height = $2,
    duration = $3,
    file_size = $4,
    format = $5,
    resolution = $6,
    thumbnail_key = $7,
    preview_key = $8,
    status = $9,
    updated_at = now()
WHERE id = $10
RETURNING id, title, uploader_id, status, COALESCE(category, '') AS category, tags,
          resolution, width, height, duration, file_size, format, download_count,
          storage_key, thumbnail_key, preview_key, rejection_reason, created_at, updated_at;

-- name: IncrementDownloadCount :exec
UPDATE wallpapers
SET download_count = download_count + 1
WHERE id = $1;

-- name: DeleteWallpaper :exec
DELETE FROM wallpapers WHERE id = $1;
```

- [ ] Create `server/db/queries/favorite.sql`:

```sql
-- name: CheckFavorite :one
SELECT EXISTS(
    SELECT 1 FROM favorites
    WHERE user_id = $1 AND wallpaper_id = $2
) AS exists;

-- name: InsertFavorite :exec
INSERT INTO favorites (user_id, wallpaper_id)
VALUES ($1, $2);

-- name: DeleteFavorite :exec
DELETE FROM favorites
WHERE user_id = $1 AND wallpaper_id = $2;

-- name: ListFavoritesByUser :many
SELECT w.id, w.title, w.uploader_id, w.status, COALESCE(w.category, '') AS category,
       w.tags, w.resolution, w.width, w.height, w.duration, w.file_size, w.format,
       w.download_count, w.storage_key, w.thumbnail_key, w.preview_key,
       w.rejection_reason, w.created_at, w.updated_at
FROM favorites f
JOIN wallpapers w ON w.id = f.wallpaper_id
WHERE f.user_id = $1
ORDER BY f.created_at DESC
LIMIT sqlc.arg('lim') OFFSET sqlc.arg('off');

-- name: CountFavoritesByUser :one
SELECT COUNT(*) FROM favorites WHERE user_id = $1;
```

- [ ] Create `server/db/queries/report.sql`:

```sql
-- name: CreateReport :one
INSERT INTO reports (wallpaper_id, reporter_id, reason)
VALUES ($1, $2, $3)
RETURNING id, wallpaper_id, reporter_id, reason, status, created_at;

-- name: ListPendingReports :many
SELECT id, wallpaper_id, reporter_id, reason, status, created_at
FROM reports
WHERE status = 'pending'
ORDER BY created_at DESC
LIMIT sqlc.arg('lim') OFFSET sqlc.arg('off');

-- name: CountPendingReports :one
SELECT COUNT(*) FROM reports WHERE status = 'pending';

-- name: DismissReport :exec
UPDATE reports SET status = 'dismissed' WHERE id = $1;
```

- [ ] Commit:

```bash
git add server/db/
git commit -m "feat(server): add sqlc config and SQL query files"
git push
```

---

## Task 4 — Atomic pgx/v5 swap (remove lib/pq, add pgx, generate with sqlc, rewrite migrate.go)

This is the atomic swap. Everything happens in one commit: `lib/pq` is removed, `pgx/v5` is added, sqlc generates the repository, `migrate.go` is rewritten, and `main.go` is updated. Do not split this into separate commits.

**Files:**
- Modify: `server/go.mod`
- Delete: `server/repository/user.go`, `server/repository/wallpaper.go`, `server/repository/favorite.go`, `server/repository/report.go`
- Delete (or gut): `server/repository/*_test.go` (they test `database/sql` repos; will be replaced in later plans)
- Modify: `server/migrate.go` (rewrite for pgx)
- Modify: `server/main.go` (use pgxpool, wire sqlc Querier)
- Create: `server/db/generated/` (sqlc output — committed)
- Modify: `server/handler/wallpaper.go`, `server/handler/favorite.go`, `server/handler/admin.go`, `server/handler/report.go` — update type imports from `repository.*` to `db.*`

### Steps

- [ ] Add required Go dependencies:

```bash
cd server
go get github.com/go-chi/chi/v5@latest
go get github.com/jackc/pgx/v5@latest
go get github.com/jackc/pgx/v5/pgxpool
go get github.com/golang-migrate/migrate/v4/database/pgx/v5
```

- [ ] Install sqlc and oapi-codegen as tools (add to `go.mod` as tools):

Create `server/tools.go`:

```go
//go:build tools

package tools

import (
	_ "github.com/oapi-codegen/oapi-codegen/v2/cmd/oapi-codegen"
	_ "github.com/sqlc-dev/sqlc/cmd/sqlc"
)
```

```bash
cd server
go get github.com/oapi-codegen/oapi-codegen/v2/cmd/oapi-codegen@latest
go get github.com/sqlc-dev/sqlc/cmd/sqlc@latest
go mod tidy
```

- [ ] Run sqlc generate to create `db/generated/`:

```bash
cd server
go run github.com/sqlc-dev/sqlc/cmd/sqlc generate -f db/sqlc.yaml
```

Expected output: creates these files in `server/db/generated/`:
- `db.go` — pgxpool adapter (DBTX interface)
- `models.go` — Go structs matching DB tables (User, Wallpaper, etc.)
- `querier.go` — Querier interface with all methods
- `user.sql.go` — implementations of user queries
- `wallpaper.sql.go` — implementations of wallpaper queries
- `favorite.sql.go` — implementations of favorite queries
- `report.sql.go` — implementations of report queries

If any query fails to parse, fix the SQL in `db/queries/*.sql` and re-run until clean.

- [ ] Rewrite `server/migrate.go` to use pgx:

Delete the entire current content and replace with:

```go
package main

import (
	"context"
	"embed"
	"errors"
	"fmt"

	"github.com/golang-migrate/migrate/v4"
	"github.com/golang-migrate/migrate/v4/database/pgx/v5"
	"github.com/golang-migrate/migrate/v4/source/iofs"
	"github.com/jackc/pgx/v5/pgxpool"
)

//go:embed db/migrations/*.sql
var migrationsFS embed.FS

func RunMigrations(ctx context.Context, pool *pgxpool.Pool) error {
	source, err := iofs.New(migrationsFS, "db/migrations")
	if err != nil {
		return fmt.Errorf("migration source: %w", err)
	}

	connStr := pool.Config().ConnConfig.ConnString()
	driver, err := pgx5.Open(connStr)
	if err != nil {
		return fmt.Errorf("migration driver: %w", err)
	}

	m, err := migrate.NewWithInstance("iofs", source, "pgx5", driver)
	if err != nil {
		return fmt.Errorf("migration init: %w", err)
	}

	if err := m.Up(); err != nil && !errors.Is(err, migrate.ErrNoChange) {
		return fmt.Errorf("migration up: %w", err)
	}

	return nil
}
```

> Note: `pgx5` is the import alias for `github.com/golang-migrate/migrate/v4/database/pgx/v5`. Check the package name in that module — it may be `pgx5` or `pgxv5`. Adjust the alias to match whatever the package exports.

- [ ] Delete the old `repository/` package:

```bash
cd server
rm repository/user.go repository/wallpaper.go repository/favorite.go repository/report.go
# Keep test files temporarily gutted, or delete them:
rm repository/user_test.go repository/wallpaper_test.go repository/favorite_test.go repository/report_test.go
```

- [ ] Update handler imports. Each handler file that imports `repository` must switch to `db` (the sqlc-generated package). The type names change:

| Old type (`repository.*`) | New type (`db.*`) |
|---|---|
| `repository.User` | `db.User` |
| `repository.Wallpaper` | `db.Wallpaper` |
| `repository.Report` | `db.Report` |
| `repository.UserRepo` | `db.Querier` (interface) |
| `repository.WallpaperRepo` | `db.Querier` (interface) |
| `repository.FavoriteRepo` | `db.Querier` (interface) |
| `repository.ReportRepo` | `db.Querier` (interface) |
| `repository.CreateParams` | becomes inline args |
| `repository.ListParams` | decomposed in handler |
| `repository.FinalizeParams` | decomposed in handler |

Each handler struct that holds `*repository.XxxRepo` should be updated to hold `db.Querier`. For example, `handler/admin.go`:

```go
// Before:
type AdminHandler struct {
    wallpapers *repository.WallpaperRepo
    users      *repository.UserRepo
    reports    *repository.ReportRepo
}

// After:
type AdminHandler struct {
    q db.Querier
}
```

Apply the same pattern to `handler/auth.go`, `handler/wallpaper.go`, `handler/favorite.go`, `handler/report.go`.

Update all method calls from `h.wallpapers.GetByID(...)` style to `h.q.GetWallpaperByID(...)` using the sqlc-generated method names (visible in `db/generated/querier.go`).

> At this stage the handlers may not compile perfectly if the sqlc field names differ from the old repository field names. Fix each compile error by consulting `db/generated/models.go` for the exact field names. The goal is a clean `go build ./...`.

- [ ] Rewrite `server/main.go` to use pgxpool:

```go
package main

import (
	"context"
	"log/slog"
	"net/http"
	"os"
	"os/signal"
	"syscall"
	"time"

	"github.com/jackc/pgx/v5/pgxpool"

	"github.com/0x63616c/screenspace/server/db/generated"
	"github.com/0x63616c/screenspace/server/handler"
	"github.com/0x63616c/screenspace/server/middleware"
	"github.com/0x63616c/screenspace/server/service"
	"github.com/0x63616c/screenspace/server/storage"
)

func main() {
	cfg, err := LoadConfig()
	if err != nil {
		slog.Error("config", "error", err)
		os.Exit(1)
	}

	ctx, stop := signal.NotifyContext(context.Background(), syscall.SIGINT, syscall.SIGTERM)
	defer stop()

	// Database pool
	poolCfg, err := pgxpool.ParseConfig(cfg.DatabaseURL)
	if err != nil {
		slog.Error("database config", "error", err)
		os.Exit(1)
	}
	poolCfg.MaxConns = int32(cfg.DBMaxConns)
	poolCfg.MinConns = int32(cfg.DBMinConns)
	poolCfg.MaxConnLifetime = cfg.DBMaxConnLifetime
	poolCfg.HealthCheckPeriod = cfg.DBHealthCheckPeriod

	pool, err := pgxpool.NewWithConfig(ctx, poolCfg)
	if err != nil {
		slog.Error("database pool", "error", err)
		os.Exit(1)
	}
	defer pool.Close()

	if err := pool.Ping(ctx); err != nil {
		slog.Error("database ping", "error", err)
		os.Exit(1)
	}

	if err := RunMigrations(ctx, pool); err != nil {
		slog.Error("migrations", "error", err)
		os.Exit(1)
	}

	// Querier
	q := db.New(pool)

	// S3 Storage
	store, err := storage.NewS3Store(cfg.S3Endpoint, cfg.S3Bucket, cfg.S3AccessKey, cfg.S3SecretKey)
	if err != nil {
		slog.Error("storage", "error", err)
		os.Exit(1)
	}
	if err := store.EnsureBucket(ctx); err != nil {
		slog.Warn("could not ensure bucket", "error", err)
	}

	// Services
	authService := service.NewAuthService(cfg.JWTSecret)
	videoService := service.NewVideoService()

	// Handlers
	authHandler := handler.NewAuthHandler(q, authService, cfg.AdminEmail)
	wallpaperHandler := handler.NewWallpaperHandler(q, store, videoService, authService)
	favoriteHandler := handler.NewFavoriteHandler(q)
	reportHandler := handler.NewReportHandler(q)
	adminHandler := handler.NewAdminHandler(q)

	// Middleware
	authMw := middleware.Auth(authService)
	uploadLimiter := middleware.NewRateLimiter(cfg.UploadRateLimit)

	// Router (still net/http.ServeMux — chi migration is a later plan)
	mux := http.NewServeMux()

	mux.HandleFunc("GET /health", func(w http.ResponseWriter, r *http.Request) {
		w.Header().Set("Content-Type", "application/json")
		w.WriteHeader(http.StatusOK)
		w.Write([]byte(`{"status":"ok"}`))
	})
	mux.HandleFunc("POST /api/v1/auth/register", authHandler.Register)
	mux.HandleFunc("POST /api/v1/auth/login", authHandler.Login)
	mux.HandleFunc("GET /api/v1/categories", handler.ListCategories)
	mux.HandleFunc("GET /api/v1/wallpapers", wallpaperHandler.List)
	mux.HandleFunc("GET /api/v1/wallpapers/popular", wallpaperHandler.Popular)
	mux.HandleFunc("GET /api/v1/wallpapers/recent", wallpaperHandler.Recent)
	mux.HandleFunc("GET /api/v1/wallpapers/{id}", wallpaperHandler.Get)

	mux.Handle("GET /api/v1/auth/me", authMw(http.HandlerFunc(authHandler.Me)))
	mux.Handle("POST /api/v1/wallpapers", authMw(uploadLimiter.Middleware(http.HandlerFunc(wallpaperHandler.Create))))
	mux.Handle("POST /api/v1/wallpapers/{id}/download", authMw(http.HandlerFunc(wallpaperHandler.Download)))
	mux.Handle("POST /api/v1/wallpapers/{id}/finalize", authMw(http.HandlerFunc(wallpaperHandler.Finalize)))
	mux.Handle("DELETE /api/v1/wallpapers/{id}", authMw(http.HandlerFunc(wallpaperHandler.Delete)))
	mux.Handle("POST /api/v1/wallpapers/{id}/favorite", authMw(http.HandlerFunc(favoriteHandler.Toggle)))
	mux.Handle("GET /api/v1/me/favorites", authMw(http.HandlerFunc(favoriteHandler.List)))
	mux.Handle("POST /api/v1/wallpapers/{id}/report", authMw(http.HandlerFunc(reportHandler.Create)))

	mux.Handle("GET /api/v1/admin/queue", authMw(middleware.Admin(http.HandlerFunc(adminHandler.Queue))))
	mux.Handle("POST /api/v1/admin/queue/{id}/approve", authMw(middleware.Admin(http.HandlerFunc(adminHandler.Approve))))
	mux.Handle("POST /api/v1/admin/queue/{id}/reject", authMw(middleware.Admin(http.HandlerFunc(adminHandler.Reject))))
	mux.Handle("GET /api/v1/admin/wallpapers", authMw(middleware.Admin(http.HandlerFunc(adminHandler.ListWallpapers))))
	mux.Handle("PATCH /api/v1/admin/wallpapers/{id}", authMw(middleware.Admin(http.HandlerFunc(adminHandler.EditWallpaper))))
	mux.Handle("GET /api/v1/admin/users", authMw(middleware.Admin(http.HandlerFunc(adminHandler.ListUsers))))
	mux.Handle("POST /api/v1/admin/users/{id}/ban", authMw(middleware.Admin(http.HandlerFunc(adminHandler.BanUser))))
	mux.Handle("POST /api/v1/admin/users/{id}/unban", authMw(middleware.Admin(http.HandlerFunc(adminHandler.UnbanUser))))
	mux.Handle("POST /api/v1/admin/users/{id}/promote", authMw(middleware.Admin(http.HandlerFunc(adminHandler.PromoteUser))))
	mux.Handle("GET /api/v1/admin/reports", authMw(middleware.Admin(http.HandlerFunc(adminHandler.ListReports))))
	mux.Handle("POST /api/v1/admin/reports/{id}/dismiss", authMw(middleware.Admin(http.HandlerFunc(adminHandler.DismissReport))))

	srv := &http.Server{
		Addr:         ":" + cfg.Port,
		Handler:      mux,
		ReadTimeout:  15 * time.Second,
		WriteTimeout: 30 * time.Second,
		IdleTimeout:  120 * time.Second,
	}

	go func() {
		slog.Info("listening", "port", cfg.Port)
		if err := srv.ListenAndServe(); err != nil && err != http.ErrServerClosed {
			slog.Error("server", "error", err)
			os.Exit(1)
		}
	}()

	<-ctx.Done()
	slog.Info("shutting down", "signal", ctx.Err())
	shutdownCtx, cancel := context.WithTimeout(context.Background(), cfg.ShutdownTimeout)
	defer cancel()
	if err := srv.Shutdown(shutdownCtx); err != nil {
		slog.Error("shutdown", "error", err)
	}
}
```

- [ ] Remove `lib/pq` from `go.mod` and tidy:

```bash
cd server
go mod edit -droprequire github.com/lib/pq
go mod tidy
```

Verify `lib/pq` is gone:

```bash
grep "lib/pq" go.mod go.sum
# Expected: no output
```

- [ ] Verify build:

```bash
cd server
go build ./...
# Expected: no output (clean build)
```

If there are compile errors, they will be in the handler files due to type mismatches. Fix each by consulting `db/generated/models.go` and `db/generated/querier.go`. Do not proceed until `go build ./...` is clean.

- [ ] Run existing tests:

```bash
cd server
go test -race ./...
```

Some tests may fail because they used `database/sql`. Note which ones fail — they will be replaced by service-layer tests in a later plan. The build must be clean before committing.

- [ ] Commit (atomic — everything pgx-related in one commit):

```bash
git add server/go.mod server/go.sum
git add server/tools.go
git add server/migrate.go
git add server/main.go
git add server/db/generated/
git add server/handler/
git rm server/repository/user.go server/repository/wallpaper.go server/repository/favorite.go server/repository/report.go
git rm server/repository/user_test.go server/repository/wallpaper_test.go server/repository/favorite_test.go server/repository/report_test.go
git commit -m "refactor(server): atomic pgx/v5 swap — remove lib/pq, add pgxpool, generate repos with sqlc"
git push
```

---

## Task 5 — Config centralization

**Files:**
- Create: `server/internal/config/config.go`
- Modify: `server/config.go` (replace with delegation shim, then delete in next step)
- Modify: `server/main.go` (update import path)

### Steps

- [ ] Create `server/internal/config/config.go`:

```go
package config

import (
	"errors"
	"fmt"
	"os"
	"strconv"
	"time"
)

// Config holds all server configuration loaded from environment variables.
// Every field maps 1:1 to an env var documented in the comment.
type Config struct {
	// Server
	Port            string        // PORT (default "8080")
	ShutdownTimeout time.Duration // SHUTDOWN_TIMEOUT (default 25s)
	LogLevel        string        // LOG_LEVEL (default "info")

	// Database
	DatabaseURL         string        // DATABASE_URL (required)
	DBMaxConns          int           // DB_MAX_CONNS (default 25)
	DBMinConns          int           // DB_MIN_CONNS (default 5)
	DBMaxConnLifetime   time.Duration // DB_MAX_CONN_LIFETIME (default 5m)
	DBHealthCheckPeriod time.Duration // DB_HEALTH_CHECK_PERIOD (default 30s)

	// S3
	S3Endpoint  string // S3_ENDPOINT (required)
	S3Bucket    string // S3_BUCKET (default "screenspace")
	S3AccessKey string // S3_ACCESS_KEY (required)
	S3SecretKey string // S3_SECRET_KEY (required)

	// Auth
	JWTSecret      string        // JWT_SECRET (required, min 32 chars)
	JWTExpiry      time.Duration // JWT_EXPIRY (default 168h = 7d)
	AdminEmail     string        // ADMIN_EMAIL
	BcryptCost     int           // BCRYPT_COST (default 10)
	MinPasswordLen int           // MIN_PASSWORD_LENGTH (default 8)

	// Rate Limits
	AuthRateLimit     int // AUTH_RATE_LIMIT (default 10)
	PublicRateLimit   int // PUBLIC_RATE_LIMIT (default 120)
	UserRateLimit     int // USER_RATE_LIMIT (default 30)
	UploadRateLimit   int // UPLOAD_RATE_LIMIT (default 5)
	DownloadRateLimit int // DOWNLOAD_RATE_LIMIT (default 60)

	// Upload Constraints
	MaxFileSize     int64   // MAX_FILE_SIZE (default 209715200 = 200MB)
	MaxDuration     float64 // MAX_DURATION (default 60.0)
	MinHeight       int     // MIN_HEIGHT (default 1080)
	MaxTitleLength  int     // MAX_TITLE_LENGTH (default 255)
	MaxTagCount     int     // MAX_TAG_COUNT (default 10)
	MaxTagLength    int     // MAX_TAG_LENGTH (default 50)
	MaxReportLength int     // MAX_REPORT_LENGTH (default 500)

	// Pagination
	DefaultPageSize int // DEFAULT_PAGE_SIZE (default 20)
	MaxPageSize     int // MAX_PAGE_SIZE (default 100)

	// Presigned URL Expiry
	PresignedDownloadExpiry time.Duration // PRESIGNED_DOWNLOAD_EXPIRY (default 1h)
	PresignedUploadExpiry   time.Duration // PRESIGNED_UPLOAD_EXPIRY (default 2h)
}

// Load reads configuration from environment variables and returns a validated Config.
// Missing required fields or invalid values cause an error.
func Load() (*Config, error) {
	cfg := &Config{
		Port:            envStr("PORT", "8080"),
		ShutdownTimeout: envDuration("SHUTDOWN_TIMEOUT", 25*time.Second),
		LogLevel:        envStr("LOG_LEVEL", "info"),

		DatabaseURL:         os.Getenv("DATABASE_URL"),
		DBMaxConns:          envInt("DB_MAX_CONNS", 25),
		DBMinConns:          envInt("DB_MIN_CONNS", 5),
		DBMaxConnLifetime:   envDuration("DB_MAX_CONN_LIFETIME", 5*time.Minute),
		DBHealthCheckPeriod: envDuration("DB_HEALTH_CHECK_PERIOD", 30*time.Second),

		S3Endpoint:  os.Getenv("S3_ENDPOINT"),
		S3Bucket:    envStr("S3_BUCKET", "screenspace"),
		S3AccessKey: os.Getenv("S3_ACCESS_KEY"),
		S3SecretKey: os.Getenv("S3_SECRET_KEY"),

		JWTSecret:      os.Getenv("JWT_SECRET"),
		JWTExpiry:      envDuration("JWT_EXPIRY", 7*24*time.Hour),
		AdminEmail:     os.Getenv("ADMIN_EMAIL"),
		BcryptCost:     envInt("BCRYPT_COST", 10),
		MinPasswordLen: envInt("MIN_PASSWORD_LENGTH", 8),

		AuthRateLimit:     envInt("AUTH_RATE_LIMIT", 10),
		PublicRateLimit:   envInt("PUBLIC_RATE_LIMIT", 120),
		UserRateLimit:     envInt("USER_RATE_LIMIT", 30),
		UploadRateLimit:   envInt("UPLOAD_RATE_LIMIT", 5),
		DownloadRateLimit: envInt("DOWNLOAD_RATE_LIMIT", 60),

		MaxFileSize:     envInt64("MAX_FILE_SIZE", 200*1024*1024),
		MaxDuration:     envFloat64("MAX_DURATION", 60.0),
		MinHeight:       envInt("MIN_HEIGHT", 1080),
		MaxTitleLength:  envInt("MAX_TITLE_LENGTH", 255),
		MaxTagCount:     envInt("MAX_TAG_COUNT", 10),
		MaxTagLength:    envInt("MAX_TAG_LENGTH", 50),
		MaxReportLength: envInt("MAX_REPORT_LENGTH", 500),

		DefaultPageSize: envInt("DEFAULT_PAGE_SIZE", 20),
		MaxPageSize:     envInt("MAX_PAGE_SIZE", 100),

		PresignedDownloadExpiry: envDuration("PRESIGNED_DOWNLOAD_EXPIRY", time.Hour),
		PresignedUploadExpiry:   envDuration("PRESIGNED_UPLOAD_EXPIRY", 2*time.Hour),
	}

	return cfg, cfg.validate()
}

func (c *Config) validate() error {
	var errs []error

	if c.DatabaseURL == "" {
		errs = append(errs, errors.New("DATABASE_URL is required"))
	}
	if c.JWTSecret == "" {
		errs = append(errs, errors.New("JWT_SECRET is required"))
	} else if len(c.JWTSecret) < 32 {
		errs = append(errs, fmt.Errorf("JWT_SECRET must be at least 32 characters (got %d)", len(c.JWTSecret)))
	}
	if c.S3Endpoint == "" {
		errs = append(errs, errors.New("S3_ENDPOINT is required"))
	}
	if c.S3AccessKey == "" {
		errs = append(errs, errors.New("S3_ACCESS_KEY is required"))
	}
	if c.S3SecretKey == "" {
		errs = append(errs, errors.New("S3_SECRET_KEY is required"))
	}
	if c.DBMaxConns < 1 {
		errs = append(errs, errors.New("DB_MAX_CONNS must be >= 1"))
	}
	if c.DBMinConns < 0 {
		errs = append(errs, errors.New("DB_MIN_CONNS must be >= 0"))
	}
	if c.DBMinConns > c.DBMaxConns {
		errs = append(errs, errors.New("DB_MIN_CONNS must be <= DB_MAX_CONNS"))
	}
	if c.MaxPageSize < c.DefaultPageSize {
		errs = append(errs, errors.New("MAX_PAGE_SIZE must be >= DEFAULT_PAGE_SIZE"))
	}

	return errors.Join(errs...)
}

// envStr returns the env var value or the default if unset/empty.
func envStr(key, def string) string {
	if v := os.Getenv(key); v != "" {
		return v
	}
	return def
}

// envInt returns the env var parsed as int, or the default on parse failure or absence.
func envInt(key string, def int) int {
	v := os.Getenv(key)
	if v == "" {
		return def
	}
	n, err := strconv.Atoi(v)
	if err != nil {
		return def
	}
	return n
}

// envInt64 returns the env var parsed as int64, or the default on parse failure or absence.
func envInt64(key string, def int64) int64 {
	v := os.Getenv(key)
	if v == "" {
		return def
	}
	n, err := strconv.ParseInt(v, 10, 64)
	if err != nil {
		return def
	}
	return n
}

// envFloat64 returns the env var parsed as float64, or the default on parse failure or absence.
func envFloat64(key string, def float64) float64 {
	v := os.Getenv(key)
	if v == "" {
		return def
	}
	f, err := strconv.ParseFloat(v, 64)
	if err != nil {
		return def
	}
	return f
}

// envDuration returns the env var parsed as time.Duration, or the default on parse failure or absence.
func envDuration(key string, def time.Duration) time.Duration {
	v := os.Getenv(key)
	if v == "" {
		return def
	}
	d, err := time.ParseDuration(v)
	if err != nil {
		return def
	}
	return d
}
```

- [ ] Write a test for `internal/config/config.go`. Create `server/internal/config/config_test.go`:

```go
package config_test

import (
	"testing"

	"github.com/0x63616c/screenspace/server/internal/config"
)

func TestLoad_MissingRequired(t *testing.T) {
	// Unset all required vars
	t.Setenv("DATABASE_URL", "")
	t.Setenv("JWT_SECRET", "")
	t.Setenv("S3_ENDPOINT", "")
	t.Setenv("S3_ACCESS_KEY", "")
	t.Setenv("S3_SECRET_KEY", "")

	_, err := config.Load()
	if err == nil {
		t.Fatal("expected error for missing required fields, got nil")
	}
}

func TestLoad_JWTSecretTooShort(t *testing.T) {
	t.Setenv("DATABASE_URL", "postgres://localhost/test")
	t.Setenv("JWT_SECRET", "tooshort")
	t.Setenv("S3_ENDPOINT", "http://localhost:9000")
	t.Setenv("S3_ACCESS_KEY", "key")
	t.Setenv("S3_SECRET_KEY", "secret")

	_, err := config.Load()
	if err == nil {
		t.Fatal("expected error for short JWT_SECRET, got nil")
	}
}

func TestLoad_Defaults(t *testing.T) {
	t.Setenv("DATABASE_URL", "postgres://localhost/test")
	t.Setenv("JWT_SECRET", "a-very-long-secret-that-is-at-least-32-chars!!")
	t.Setenv("S3_ENDPOINT", "http://localhost:9000")
	t.Setenv("S3_ACCESS_KEY", "key")
	t.Setenv("S3_SECRET_KEY", "secret")

	cfg, err := config.Load()
	if err != nil {
		t.Fatalf("unexpected error: %v", err)
	}
	if cfg.Port != "8080" {
		t.Errorf("expected default port 8080, got %s", cfg.Port)
	}
	if cfg.DBMaxConns != 25 {
		t.Errorf("expected default DBMaxConns 25, got %d", cfg.DBMaxConns)
	}
	if cfg.MaxFileSize != 200*1024*1024 {
		t.Errorf("expected default MaxFileSize 200MB, got %d", cfg.MaxFileSize)
	}
	if cfg.UploadRateLimit != 5 {
		t.Errorf("expected default UploadRateLimit 5, got %d", cfg.UploadRateLimit)
	}
}

func TestLoad_OverrideDefaults(t *testing.T) {
	t.Setenv("DATABASE_URL", "postgres://localhost/test")
	t.Setenv("JWT_SECRET", "a-very-long-secret-that-is-at-least-32-chars!!")
	t.Setenv("S3_ENDPOINT", "http://localhost:9000")
	t.Setenv("S3_ACCESS_KEY", "key")
	t.Setenv("S3_SECRET_KEY", "secret")
	t.Setenv("PORT", "9090")
	t.Setenv("DB_MAX_CONNS", "50")
	t.Setenv("UPLOAD_RATE_LIMIT", "10")

	cfg, err := config.Load()
	if err != nil {
		t.Fatalf("unexpected error: %v", err)
	}
	if cfg.Port != "9090" {
		t.Errorf("expected port 9090, got %s", cfg.Port)
	}
	if cfg.DBMaxConns != 50 {
		t.Errorf("expected DBMaxConns 50, got %d", cfg.DBMaxConns)
	}
	if cfg.UploadRateLimit != 10 {
		t.Errorf("expected UploadRateLimit 10, got %d", cfg.UploadRateLimit)
	}
}
```

- [ ] Run the config tests (should pass without a database):

```bash
cd server
go test -race ./internal/config/...
# Expected:
# ok  	github.com/0x63616c/screenspace/server/internal/config	0.XXXs
```

- [ ] Replace `server/config.go` with a thin delegation shim that calls `internal/config.Load()`. This keeps `main.go` changes minimal for now:

```go
package main

import (
	"github.com/0x63616c/screenspace/server/internal/config"
)

// Config is re-exported from internal/config for use in main.
type Config = config.Config

// LoadConfig loads server config from environment variables.
func LoadConfig() (*Config, error) {
	return config.Load()
}
```

- [ ] Delete the old `server/config_test.go` (it tests the old flat config). It will be replaced by `internal/config/config_test.go`:

```bash
cd server
rm config_test.go
```

- [ ] Build and test:

```bash
cd server
go build ./...
go test -race ./...
```

- [ ] Commit:

```bash
git add server/internal/config/
git add server/config.go
git rm server/config_test.go
git commit -m "feat(server): centralize config in internal/config with validation and ~40 fields"
git push
```

---

## Task 6 — Write the Makefile

**Files:**
- Create: `server/Makefile`

### Steps

- [ ] Create `server/Makefile`:

```makefile
BINARY      := screenspace-server
BUILD_DIR   := bin
MODULE      := github.com/0x63616c/screenspace/server
SQLC        := go run github.com/sqlc-dev/sqlc/cmd/sqlc
OAPI        := go run github.com/oapi-codegen/oapi-codegen/v2/cmd/oapi-codegen
MIGRATE_URL ?= $(DATABASE_URL)

.PHONY: build run test lint generate migrate/up migrate/down audit clean

## build: compile the server binary
build:
	go build -o $(BUILD_DIR)/$(BINARY) ./cmd/server/

## run: build and run the server
run: build
	./$(BUILD_DIR)/$(BINARY)

## test: run all tests with race detector
test:
	go test -race ./...

## lint: run golangci-lint
lint:
	golangci-lint run ./...

## generate: run sqlc and oapi-codegen to regenerate all generated code
generate: generate/sqlc generate/oapi

generate/sqlc:
	$(SQLC) generate -f db/sqlc.yaml

generate/oapi:
	$(OAPI) --config api/cfg.yaml api/openapi.yaml

## migrate/up: run pending database migrations
migrate/up:
	@test -n "$(MIGRATE_URL)" || (echo "MIGRATE_URL or DATABASE_URL must be set" && exit 1)
	go run -tags 'pgx5' github.com/golang-migrate/migrate/v4/cmd/migrate \
		-path db/migrations \
		-database "$(MIGRATE_URL)" \
		up

## migrate/down: roll back the last migration
migrate/down:
	@test -n "$(MIGRATE_URL)" || (echo "MIGRATE_URL or DATABASE_URL must be set" && exit 1)
	go run -tags 'pgx5' github.com/golang-migrate/migrate/v4/cmd/migrate \
		-path db/migrations \
		-database "$(MIGRATE_URL)" \
		down 1

## audit: run all quality checks
audit: lint test
	go vet ./...
	go mod tidy
	@git diff --exit-code go.mod go.sum || (echo "go.mod/go.sum have uncommitted changes after tidy" && exit 1)
	$(SQLC) vet -f db/sqlc.yaml
	$(MAKE) generate
	@git diff --exit-code db/generated/ generated/ || (echo "generated files are out of date — run make generate" && exit 1)

## clean: remove build artifacts
clean:
	rm -rf $(BUILD_DIR)

## help: list available targets
help:
	@grep -E '^## ' Makefile | sed 's/## //'
```

- [ ] Verify the Makefile targets work:

```bash
cd server
make build
# Expected:
# go build -o bin/screenspace-server ./cmd/server/
# (file bin/screenspace-server created)

make test
# Expected:
# ok  github.com/0x63616c/screenspace/server/internal/config  0.XXXs
# ok  github.com/0x63616c/screenspace/server/handler          0.XXXs
# ... (some tests may require a running database and will be skipped or fail)

make generate
# Expected: regenerates db/generated/ and generated/ — git diff should be empty if no schema changes
```

- [ ] Commit:

```bash
git add server/Makefile
git commit -m "chore(server): add Makefile with build, test, lint, generate, migrate, audit targets"
git push
```

---

## Task 7 — Run oapi-codegen and commit generated server interface

**Files:**
- Create: `server/generated/server.gen.go`
- Create: `server/generated/types.gen.go`

### Steps

- [ ] Ensure oapi-codegen is resolvable:

```bash
cd server
go run github.com/oapi-codegen/oapi-codegen/v2/cmd/oapi-codegen --version
# Expected: prints version (e.g., v2.x.x)
```

- [ ] Run oapi-codegen:

```bash
cd server
make generate/oapi
# Expected: creates server/generated/server.gen.go and server/generated/types.gen.go
```

If the spec has errors, oapi-codegen will report them. Fix `api/openapi.yaml` until generation succeeds.

- [ ] Verify the generated files compile:

```bash
cd server
go build ./...
# Expected: no output
```

> The generated `ServerInterface` will list all operationId handlers. At this stage the handlers in `handler/` do NOT implement this interface yet — that is wired in Plan 2 (chi router + handler adapter). The generated code just needs to compile, not be wired.
>
> **Note:** Go 1.26 idioms like `errors.AsType[T]()` are not used in this plan (foundation/infrastructure). They will be applied in Plan 2 (Go Application Layer) where error handling patterns are built. Similarly, `omitzero` tags in oapi-codegen output default to `omitempty` — this is accepted for generated code since oapi-codegen does not yet support `omitzero`. Hand-written struct tags in Plan 2 will use `omitzero`.

- [ ] Commit:

```bash
git add server/generated/
git commit -m "feat(server): generate oapi-codegen server interface and types from OpenAPI spec"
git push
```

---

## Verification Checklist

After all tasks are complete, run the following to confirm the plan objectives are met:

```bash
cd server

# 1. Clean build
go build ./...
# Expected: no output

# 2. All tests pass (unit tests that don't need a DB)
go test -race ./internal/config/... ./handler/... ./service/... ./middleware/...
# Expected: all pass

# 3. lib/pq is gone
grep -r "lib/pq" .
# Expected: no output

# 4. No database/sql imports outside of generated code
grep -r '"database/sql"' --include="*.go" .
# Expected: no output (generated code may still use it internally — that is OK)

# 5. sqlc generated code exists
ls db/generated/
# Expected: db.go  models.go  querier.go  user.sql.go  wallpaper.sql.go  favorite.sql.go  report.sql.go

# 6. oapi-codegen generated code exists
ls generated/
# Expected: server.gen.go  types.gen.go

# 7. Config test passes standalone
go test -race -v ./internal/config/...
# Expected: 4 test functions pass

# 8. Makefile targets are present
make help
# Expected: lists build, run, test, lint, generate, migrate/up, migrate/down, audit, clean

# 9. No magic strings for status/role in non-generated code (spot check)
grep -r '"pending"' --include="*.go" . | grep -v generated | grep -v "_test.go"
# Expected: only in db/queries/*.sql and types.go constants
```

---

## Commit Summary

| # | Commit message | Scope |
|---|---|---|
| 1 | `chore(server): move migrations to db/migrations and update embed path` | scaffold |
| 2 | `feat(server): add OpenAPI 3.0 spec and oapi-codegen config` | API contract |
| 3 | `feat(server): add sqlc config and SQL query files` | queries |
| 4 | `refactor(server): atomic pgx/v5 swap — remove lib/pq, add pgxpool, generate repos with sqlc` | DB layer |
| 5 | `feat(server): centralize config in internal/config with validation and ~40 fields` | config |
| 6 | `chore(server): add Makefile with build, test, lint, generate, migrate, audit targets` | tooling |
| 7 | `feat(server): generate oapi-codegen server interface and types from OpenAPI spec` | codegen |
